## tauri v2

### rust のupdate

```sh
rustup update
```

### ubuntu

うまく行かなかったので、色々インストールしたけど、
とりあえずこれは必須だった。

```sh
sudo apt install libwebkit2gtk-4.1-dev -y
```

### packages

```sh
npm install @tauri-apps/api@">1.0.0"
```